/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'localhost', 'https://space-timelinee.vercel.app/'],
  },
}

module.exports = nextConfig
